all:
    @echo "Building the project..."
    # Add any build steps if necessary
    @echo "Build complete."

clean:
    @echo "Cleaning up..."
    # Add any clean steps if necessary
    @echo "Clean complete."
